// Debug visualizations: section/region bounds, dirty flags, sort-type coloring, upload
// budget HUD, worker queue depth. RENDERER_PLAN.md §22 (perf instrumentation), §16.

import { notImplemented } from "../../notImplemented";

export class DebugOverlays {
  render(): void {
    notImplemented("DebugOverlays.render");
  }
}
